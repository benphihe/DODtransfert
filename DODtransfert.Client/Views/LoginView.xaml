<UserControl x:Class="DODtransfert.Client.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="500">
    <Grid Margin="40">
        <StackPanel VerticalAlignment="Center">
            <TextBlock Text="Connexion" 
                       FontSize="28" FontWeight="Bold" 
                       HorizontalAlignment="Center" Margin="0,0,0,30"/>

            <TextBlock Text="Nom d'utilisateur" Margin="0,0,0,5"/>
            <TextBox Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}" 
                     Margin="0,0,0,20" Padding="5"/>

            <TextBlock Text="Adresse IP du serveur" Margin="0,0,0,5"/>
            <TextBox Text="{Binding ServerIp, UpdateSourceTrigger=PropertyChanged}" 
                     Margin="0,0,0,20" Padding="5"/>

            <CheckBox Content="Mode serveur" 
                      IsChecked="{Binding IsServerMode}"
                      Margin="0,0,0,20"/>

            <TextBlock Text="{Binding StatusMessage}" 
                       Foreground="Gray" 
                       Margin="0,0,0,20"
                       TextWrapping="Wrap"/>

            <Button Content="Se connecter" 
                    Command="{Binding LoginCommand}"
                    Padding="20,10" FontSize="14" FontWeight="Bold"
                    HorizontalAlignment="Stretch"/>
        </StackPanel>
    </Grid>
</UserControl>
